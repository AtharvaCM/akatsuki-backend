Returns a list of hotels for the given location
---
tags:
  - Hotels
parameters:
  - in: path
    name: hotel_list
    required: true

responses:
  200:
  description: Returns a list of hotels

  404:
  description: No records found