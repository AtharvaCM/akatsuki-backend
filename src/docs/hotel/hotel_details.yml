  returns details of a particular hotel
---
    
# /api/v1/hotels/:
get:
  tags:
    - Hotel details
  parameters:
    - name: hotel_details
      in: path
      required: true
  responses:
    200:
      description: returns details of a particular hotel
      schema:
        type: object
        required:
          - "id"
        properties:
          id:
            type: integer
    404:
      description: No records found
  
 











